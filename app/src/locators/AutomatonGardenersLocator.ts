import { ListLocator } from '@gamepark/react-game'

class AutomatonGardenersLocator extends ListLocator {
  coordinates = { x: -22, y: -10 }
  gap = { x: 2 }
}

export const automatonGardenersLocator = new AutomatonGardenersLocator()
